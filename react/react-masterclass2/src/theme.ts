import { DefaultTheme } from "styled-components";

const blackTheme: DefaultTheme = {
    bgColor: 'black',
    textColor: 'white',
    btnColor: 'teal'
};

const whiteTheme: DefaultTheme = {
    bgColor: 'white',
    textColor: 'black',
    btnColor: 'tomato'
};


export { blackTheme, whiteTheme };